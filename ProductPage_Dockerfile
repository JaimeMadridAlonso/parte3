FROM python:3.7.7-slim

EXPOSE 9080

#todo lo que hay en productpage lo añado en la ruta en la que está este docker
ADD practica_creativa2/bookinfo/src/productpage ./

ENV GROUP_NUMBER 48

RUN pip install -r requirements.txt

CMD ["python3", "productpage.py", "9080"]
